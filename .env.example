# Development Environment Variables
# Copy this file to .env and customize for your local development

# Server Configuration
PORT=3002
NODE_ENV=development

# JWT Secret (Change this in production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# CORS Settings
CORS_ORIGIN=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Default Admin User (change immediately after first login)
ADMIN_EMAIL=admin@example.com
ADMIN_PASSWORD=admin123

# Data Storage
DATA_DIR=./server/data

# Production Notes:
# - JWT_SECRET: Use a strong, unique secret (auto-generated on Render)
# - ADMIN_PASSWORD: Use a secure password (auto-generated on Render)
# - CORS_ORIGIN: Set to your frontend domain in production
# - PORT: Auto-assigned by hosting platform (like Render)
# - DATA_DIR: Ensure persistent storage is configured
